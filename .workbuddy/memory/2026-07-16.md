# 2026-07-16

## 代码审查：知味（ZhiWei）全栈项目
- 范围：Rag/ 后端（FastAPI + Neo4j + Milvus + DeepSeek）、docker-compose/Dockerfile/pyproject、zhiwei-web（Vite+React）。
- 关键发现（已写入 `code-review-report.md`）：
  1. 【高】`main.py:190` 调用不存在的 `self.milvus.search_similar_documents`，实际方法名是 `similarity_search` → 语义检索被 except 静默禁用，向量库建了却从不查询。修复极小。
  2. 【高】后端所有接口无认证/限流，/api/chat/stream 等会触发付费 LLM，成本滥用+DoS 风险。
  3. 【高】rag_modules/ 下 graph_rag_retrieval.py、graph_indexing.py、intelligent_query_router.py、data_preparation.py 及 generation_integration 多个方法均为未接线的遗留代码；其中 graph_rag_retrieval.py:236 用 f-string 拼 Cypher（max_depth），潜在注入。
  4. 【中】api.py:418 把原始异常 str(exc) 返回客户端（信息泄露）；.env 明文密钥（已 gitignore+dockerignore，未泄露，但建议轮换+加 .env.example）；前端 .npmrc audit=false、nginx 缺安全头、SSE JSON.parse 无 try/catch。
- 结论：整体代码质量不错（Pydantic 校验、SSE、CORS、ruff 配置、前端无 XSS）。核心激活链路 = GraphRecipeDataModule(Neo4j)+GenerationIntegrationModule+GraphHybridRetrieval。
- 用户用中文，后续回复保持中文。

## 生成代码修改计划
- 基于审查报告产出可执行改动清单：`code-fix-plan.md`。
- 分 P0–P3 四阶段：P0-1（main.py:190 改名 similarity_search + 新增契约测试，极小）、P0-2（API Key 鉴权 + slowapi 限流 + LLM request_timeout=30）；P1（清理遗留模块、Cypher 参数化、密钥治理）；P2/P3 为中低危清单。
- 含 CI 红线（ruff S608、gitleaks、付费接口先认证、集成测试兜底）防复发。

## 生成 Agent 修复 Runbook
- 用户要求把修改计划转为「供 AI 修复 Agent 直接执行」的 runbook：`agent-fix-plan.md`。
- 结构：基线准备 + T1..T17 任务（每任务含文件/操作/精确 old→new 或 grep 定位/验证命令/回滚），按依赖排序（T1→T2→T3→T4；T6→T5；T7 先查 git 历史泄露）。
- 人读版仍为 `code-fix-plan.md`，两份配套。

## 制定代码审查标准与流程（常态化制度）
- 用户要求把「一次性审查」固化成「常态化制度」。产出两份文件：
  - `CODE_REVIEW_STANDARD.md`：9 条团队红线（RL1 注入参数化 / RL2 付费接口认证限流 / RL3 密钥 / RL4 异常不外泄 / RL5 死代码标注 / RL6 核心链路集成测试 / RL7 外部调用超时+可取消 / RL8 前端安全基线 / RL9 数据权威一致）+ 角色职责 + 严重程度(🔴🟡💭) + 分域审查清单 + 流程 + CI 加固清单 + 反面案例。
  - `.github/PULL_REQUEST_TEMPLATE.md`：团队 PR 模板，含安全自查（RL1-9 逐项勾选）与测试证据。
- 关键发现：现有 CI 有 gap——ruff 未开 S608（注入）、CI 未跑 gitleaks（pre-commit 有但 CI 没跑）、前端测试 `npm test` 未进 CI、前端 `npm audit` 未进 CI、main 分支无审批数保护。C1-C6 列出补齐动作。
- 红线均回溯映射到 code-review-report.md 的具体发现，确保标准「源于真实事故」而非空谈。
